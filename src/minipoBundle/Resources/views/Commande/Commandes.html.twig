{% extends 'base.html.twig' %}

    {% block title %}Commandes{% endblock %}
    {% block content %}

        {% if cmd==NULL  %}
            Pas de formation
        {% else %}

            <table border="1">
                <tr>
                    <th>Reférence</th>
                    <th>Client</th>
                    <th>Date Commande</th>
                    <th>Etat Commande</th>
                    <th>Total</th>
                    <th>Action</th>
                </tr>
                {% for elt in cmd %}
                    <tr>
                        <td>{{ elt.refc }}</td>
                        {#<td>{{ elt.id.firstname ~" "~ elt.id.lastname }}</td>#}<td>{{ elt.refc }}</td>
                        <td>{{ elt.datec|date}}</td>
                        <td>{{ elt.etatc }}</td>
                        <td>{{ elt.total }}</td>
                        <td>

                            <a href=" {{ path('minipo_accepterCmd',{'id':elt.idcmd} )  }}" >
                                <button>Accepter</button>
                            </a>
                            <a href=" {{ path('minipo_refuserCmd',{'id':elt.idcmd} )  }}" >
                                <button>Refuser</button>
                            </a>

                        </td>
                    </tr>
                {% endfor %}

            </table>
        {% endif %}



    {% endblock %}

